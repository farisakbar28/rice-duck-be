# Stale-Semantics Audit — Model C

Audit command and scope: repository-wide ripgrep for the terms listed in task
section 30, excluding generated bytecode and SQLite files.

| Classification | Retained matches | Rationale |
| --- | --- | --- |
| Historical changelog | `CHANGELOG.md` release 2.0.0 | Clearly labelled historical and superseded by Model C. |
| Research candidate documentation | Model C SoT | C1–C4 and Xiong are research provenance only; the document explicitly excludes them from production. |
| Persistence legacy | `app/core/database.py` | v1–v3 columns remain solely for non-destructive physical preservation and are never returned by current Model C history endpoints. |
| Research archive | `app/engines/impact_engine.py` | Minimal archive-only notice; it contains no numerical formula and no `/dss/simulate` dependency imports it. |
| Removed optimizer | No active route, schema, OpenAPI tag, or documentation contract | `/api/v1/optimizer/*` is unmounted; historical implementation remains only in Git history. |
| Test/assertion absence | `tests/test_model_c.py`, runtime validator | References are assertions that forbidden semantics are absent, not active output fields. |
| Active Model C code | none | No production engine, service, schema, route, or visualization dependency uses the listed stale mathematical semantics. |

The former `tests/fixtures/historical_replay.json` was removed because its H01–H11 rows did not match the frozen Model C holdout rows in `docs/tes_skenario.md` and it had no active consumer.

## Holdout source-price provenance

The H01–H11 fixture restores cycle-specific `p_duck_buy` by joining
`DSS_Padi_Bebek_Rekap_Bersih_v10(1).xlsx` / `Dataset Actual Bersih` on
`Excel Row (Sumber)`, using `Buy Price Duck (Rp/ekor)`. The source row, rather
than farmer name, is the identity because farmers can have multiple cycles.
The runtime validator sends these values—including explicit zero values—and
audits `cost_duck_buy` plus scenario cash contribution against source-derived
arithmetic. This fixture correction does not affect frozen yield parameters,
holdout composition, or yield metrics.

## Holdout planting-date provenance

The same workbook/sheet and source-row identity supply `Tanggal Tanam
(Sumber)` for exactly H07/raw row 38 (`2024-04-22`), H08/raw row 43
(`2024-10-01`), and H09/raw row 44 (`2024-09-28`). The real-HTTP runner sends
only those three dates and records the returned release/withdraw windows.
All other holdout records preserve absent planting dates as omitted request
fields and `null` response date fields. This is provenance replay only, not a
model input for calibration, tuning, yield, or economics.
